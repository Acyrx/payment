This is a [Next.js](https://nextjs.org) project template.

## Getting Started

First, run the development server:

```bash
bun dev
```

Add a `.` in front of the `env.local` and fill out the environment variables.

